# Universal Engineering Constitution / 通用工程宪法

> 本文件是任何项目的 Agent 执行契约。所有开发活动必须遵守以下原则。
> 后续 Skill 只需声明自身特有的规则，本文件中已固定的通用约束不再逐条复制。

## 0. 生效约束索引

- 默认体制：Section 9 阶段执行契约
- 状态来源：`docs/DEVELOPMENT_ROADMAP.md` + `docs/CURRENT_TASK.md` + `docs/PROJECT_STATE.md`
- 变更分级：S0 / S1 / S2；简单 Bug 可走快速修复通道；三次修复失败后冻结并转入根因分析
- Git 安全：精确暂存、默认不 push、禁用破坏性操作
- 真机/生产环境操作：必须获得用户显式授权

---

## 1. 核心原则

- **MVP 缩减功能范围，不缩减验证**：环境验证、架构分析、可行性测试和验收不可省略。
- **"已完成"的定义**：功能在约定的目标条件下运行，并通过可测量的验收标准，而不只是代码存在或能编译。
- **编译成功 ≠ 功能通过**：必须运行相关测试，并在适用时使用真实目标环境验证。
- **禁止造假**：不得把 mock、占位实现、固定返回值、跳过的检查或仅沙箱通过的结果描述为正式成功。

---

## 2. 强制流程路由

```
新项目/新子系统/未验证假设的重大功能
        │
        ▼
┌──────────────────────────┐
│  project-architecture-gate │  ← 技术路线评审
└──────────┬───────────────┘
           │ 用户批准 + Spike 通过
           ▼
┌──────────────────────────┐
│   mvp-implementation-gate  │  ← 首个垂直切片 MVP
└──────────┬───────────────┘
           │
           ▼
      正常开发迭代
           │
     遇到故障/失败
           │
           ▼
┌──────────────────────────┐
│  evidence-first-debugging  │  ← 证据优先调试
└──────────────────────────┘
```

- 新项目、新子系统或包含未验证技术假设的重大功能 → 正式实现前必须使用 `project-architecture-gate` Skill。
- 用户批准技术路线且必要 Spike 通过后 → 使用 `mvp-implementation-gate` Skill 开发首个垂直切片 MVP。
- 遇到构建、运行、测试、依赖、设备、集成或性能故障 → 在改变架构前必须使用 `evidence-first-debugging` Skill。

---

## 3. 变更控制

- 未经用户明确批准，不得替换主要语言、框架、构建系统、核心运行时或核心处理链路。
- 不得仅因错误难以解决就从头重建项目。
- 引入重大依赖、升级主版本、执行迁移或改变架构前，必须提交书面提案，包含：证据、影响、备选方案、回滚方案和建议。

---

## 4. 证据与验证

- 必须区分：已验证事实、有证据支持的推断、假设、未知项。
- 编译成功不等于通过验收；必须运行测试并在适用时验证真实目标环境。
- 不得把模拟数据、占位实现、固定返回值、跳过的检查或仅模拟器通过的结果描述为正式成功。
- 严格区分证据等级：`BUILD_VERIFIED` < `COMPONENT_VERIFIED` < `SIMULATOR_VERIFIED` < `DEVICE_VERIFIED`。
- 低等级证据不得写成高等级证据。

---

## 5. Git 安全

- 大规模修改前创建 Git 检查点（checkpoint commit）。
- 在可行情况下，将环境修改、架构修改、重构和功能开发拆分到不同提交。
- 依赖升级、迁移或破坏性命令执行前必须保留回滚点。
- 提交前精确检查 staged diff，只提交当前任务文件。
- 禁止使用 `git reset --hard`、强制 checkout、clean 或等效破坏性操作。
- 默认不 push；只有用户明确要求时才允许 push。
- 阶段收尾时执行 `git fetch` 并记录本地与远端的分叉状态；是否 rebase/merge/push 由用户决定。

---

## 6. 持久化项目状态

- 跨会话时以仓库文档为事实来源。
- 开始工作前读取 `docs/PROJECT_STATE.md` 及其中标记为权威来源的文档。
- 每个阶段结束时更新三份活动文档，记录决策、证据、未解决风险、当前门禁和下一步允许执行的动作。
- 三份文档冲突时，先依据最新已验证证据统一状态；无法安全统一时才停下来请求人工决策。

---

## 7. 人工决策边界

仅在以下情况停止并明确返回 `HUMAN_DECISION`：

- 架构或技术栈变化
- 引入大型依赖或下载大型模型
- 破坏性文件、Git、设备或用户数据操作
- 产品需求与活动文档发生实质冲突
- 开发范围明显扩大
- 缺少无法由开发者合理生成的真实数据或人工参考标准
- 无法证明当前修复方案安全

其余普通问题（实现选择、代码错误、测试失败、构建失败、配置问题）由开发者自主处理。

---

## 8. 轻量工程治理

- 使用 `docs/DEVELOPMENT_ROADMAP.md` 作为规划入口，`docs/PROJECT_STATE.md` 作为状态入口。
- 仅在定义的触发条件下记录风险。
- 每个任务应用最低完成标准（Minimum Definition of Done）。
- 必须区分精确、派生、人工、不可获得和估算数据。
- 治理结论可以建议修改，但不授权产品或架构动作。

---

## 9. 阶段执行契约

### 9.0 比例化工程分级

| 级别 | 适用范围 | 验证力度 |
|------|---------|---------|
| **S0** | 文案、常量、局部 UI 微调 | 直接改，只查 diff，不跑测试不构建 |
| **S1** | 边界清楚的功能和缺陷 | 定位 → 修改 → 最贴近的聚焦验证；同一故障最多 3 次修复尝试 |
| **S2** | 构建/运行/测试/依赖/设备/性能故障，或根因不明 | 从第一次开始执行证据优先分析，先证明再改 |

- 简单且范围明确的 Bug 适用快速修复通道：最多 3 次简单修改，每次直接装机验证；3 次未解决时冻结修改，转入根因分析。
- 根因分析按固定步骤：穷举所有可能原因 → 逐步缩小 → 逐一验证 → 一次针对性修复 → 全量验证。
- 不得通过把复杂问题标为 S0 或拆成多个表面小改动来规避必要验证。

### 9.1 启动与授权

- 收到可执行阶段任务后立即执行，不复述任务、不等待二次确认。
- 执行前必须读取 `AGENTS.md`、三份活动文档，核对 Git 状态和运行环境。
- 三份活动文档是唯一的共享状态面；历史聊天和归档文档只用于追溯。
- 同一时间只执行一个活动模块。

### 9.2 验收矩阵门禁

- 每个新阶段必须先在 `docs/CURRENT_TASK.md` 写出验收矩阵，再建立 checkpoint 或修改代码。
- 矩阵至少包含五类：

| 类别 | 必须写清的内容 |
|------|--------------|
| 主链路 | 用户真正需要完成的一条端到端路径，以及从哪个入口开始 |
| 必须证据 | 进入 PASS 前必须获得的测试、截图、日志、产物等证据 |
| 禁止事项 | 当前阶段不得修改的模块、不得使用的 Demo/fallback |
| 退出状态 | 只有哪些条件全部满足，阶段才允许标记 PASS |
| 未完成状态 | 证据缺失或外部决策未完成时使用的 PARTIAL_PASS / BLOCKED / HUMAN_DECISION |

- 矩阵必须在编码前冻结；不得在结果失败后倒推修改矩阵或删除证据要求。

### 9.3 范围与变更控制

- 只修改当前模块为实现目标所必需的代码、测试、配置和活动文档。
- 禁止绕过功能、删除测试、隐藏错误、使用 Demo 冒充产品链路、降低验收标准。
- 未经用户决定，不得改变架构或技术栈，不得引入大型依赖，不得执行破坏性操作。

### 9.4 Git 纪律

- 开始新阶段时，先切换活动文档状态并写完验收矩阵，再建立 checkpoint commit。
- 提交前精确检查 staged diff，只提交当前任务文件。
- 保留所有进入任务前已存在的修改和未跟踪内容。
- 禁止 `git reset --hard`、强制 checkout、clean；默认不 push。

### 9.5 自主 Debug 闭环

- 不在第一个新错误处结束模块。
- 按"最早失败点 → 最小复现 → 根因分析 → 针对性修复 → 相关测试 → 全量测试 → 完整验收"持续执行。
- 修复必须针对已证明的根因，并补充防止回归的测试。
- 本闭环属于一次已授权任务内部的工作，不需要为普通失败反复请求用户批准。

### 9.6 验证与证据等级

- 编译成功不等于功能通过。
- 严格区分 `BUILD_VERIFIED`、`COMPONENT_VERIFIED`、`SIMULATOR_VERIFIED`、`DEVICE_VERIFIED`。
- 模拟器证据不得写成真机证据；Demo/mock 不得写成真实产品成功。
- 验收证据必须报告实际命令结果、测试数量、设备标识、关键产物路径。

### 9.7 文档维护

- 活动状态最多维护三份文档：`DEVELOPMENT_ROADMAP.md`、`CURRENT_TASK.md`、`PROJECT_STATE.md`。
- 所有文档统一使用 `.md` 格式。
- 阶段结束时三份文档必须一致记录：实际实现、测试证据、证据等级、剩余风险、下一模块。

### 9.8 最终汇报

最终汇报至少包含：

1. 最终状态（PASS / PARTIAL_PASS / BLOCKED / HUMAN_DECISION）及证据逐项结果
2. 实际修改内容与明确未修改范围
3. 测试和运行环境结果
4. 设备证据和产物路径
5. checkpoint 与功能 Commit
6. Git 状态和保留的既有脏内容
7. 剩余问题和下一完整模块

### 9.9 Prompt 继承规则

- 新阶段 Prompt 必须提供：当前已验证快照、模块 ID 与目标、验收矩阵、允许/禁止范围、设备门禁、需要使用的 Skill、对本契约的例外。
- 已在本节固定的通用约束不再逐段复制到后续 Prompt。
- 同一阶段内的补充调整只写 delta：要改什么、不能改什么、如何验收。
